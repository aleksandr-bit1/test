forms:                    # Формы
  - entity:               # Сущности для которых форма релевантна (см. PlantUML)
      - component         # В данном случае, это компонент
    fields:               # Поля формы
      repository:
        title: Репозиторий
        required: false
      project:
        title: Проект
        required: false
      description:        # Идентификатор поля
        title: Описание       # Название поля
        required: false    # Признак обязательности
      url:
        title: URL
        required: false
